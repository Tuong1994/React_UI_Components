import React from "react";
import { UI } from "@/components";
import "./style/main.scss";

const { Section } = UI;

function App() {
  return (
    <React.Fragment>
      <Section>
        
      </Section>
    </React.Fragment>
  );
}

export default App;
